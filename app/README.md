# RenderScript Studio UI (MVP)

## Setup

```bash
python3 -m pip install -e .
python3 -m pip install -r app/requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```

Open:

`http://127.0.0.1:8000`
